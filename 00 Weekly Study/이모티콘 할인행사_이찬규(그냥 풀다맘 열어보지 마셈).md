```python
from collections import deque

def solution(users, emoticons):
    emoticon, profit = 0, 0
    users.sort(key=lambda x : x[0])
    discount = deque()
    for i in range(len(users)):
        discount.append(users[i])
    answer = [emoticon, profit]
    return answer
```